import { defineConfig } from "unocss";

export default defineConfig({
	// ...UnoCSS options
	shortcuts: {
		"page-container": "w-screen h-screen overflow-hidden",
		center: "flex justify-center items-center"
	}
});
